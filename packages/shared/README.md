# FireBuddy Shared Package

This package is the start of the shared contract layer for FireBuddy.

Use it for:
- Shared TypeScript types
- Shared constants and route definitions
- Future Supabase helpers, API wrappers, and reusable hooks

Keep this package framework-light so both web and mobile can consume it cleanly.
