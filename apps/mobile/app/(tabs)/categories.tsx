import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function CategoriesScreen() {
  return (
    <PlaceholderScreen
      title="Categories"
      description="Database-backed categories will come later. For this pass, Add Expense already uses the intended shared category options and selection states."
    />
  );
}
