import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function ProfileScreen() {
  return (
    <PlaceholderScreen
      title="Profile"
      description="The profile surface is still a placeholder. The navigation shell is now aligned to the intended product tabs while Add Expense gets the detailed implementation work."
    />
  );
}
